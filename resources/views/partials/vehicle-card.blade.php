<div class="card h-100 overflow-hidden"><div class="vehicle-img d-flex align-items-center justify-content-center">@if($vehicle->image)<img src="{{ asset('storage/'.$vehicle->image) }}" class="w-100 h-100 object-fit-cover" alt="{{ $vehicle->display_name }}">@else<i class="bi {{ $vehicle->vehicle_type==='car'?'bi-car-front':'bi-scooter' }} display-2 text-ec opacity-50"></i>@endif</div><div class="card-body"><div class="d-flex justify-content-between"><span class="badge text-bg-success-subtle text-success">{{ $vehicle->vehicle_type==='car'?'Mobil':'Motor' }}</span><span class="small text-muted">{{ $vehicle->year }}</span></div><h5 class="mt-2 mb-1">{{ $vehicle->manufacturer->name }} {{ $vehicle->model }}</h5><p class="text-muted small">{{ $vehicle->variant }} · {{ $vehicle->fuel_type }}</p><h5 class="text-ec">Rp {{ number_format($vehicle->new_price,0,',','.') }}</h5><div class="d-flex gap-2 mt-3"><a href="{{ route('vehicles.show',$vehicle) }}" class="btn btn-outline-success flex-grow-1">Detail</a>@auth<form method="POST" action="{{ route('favorites.toggle',$vehicle) }}">@csrf<button class="btn btn-light" title="Favorit"><i class="bi bi-heart"></i></button></form>@endauth</div></div></div>

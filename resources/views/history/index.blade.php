@extends('layouts.app')
@section('title','Riwayat')
@section('content')<div class="container py-5"><h1 class="fw-bold">Riwayat Perbandingan</h1><p class="text-muted">Semua keputusan yang pernah Anda simpan.</p><div class="card overflow-hidden"><div class="table-responsive"><table class="table mb-0"><thead class="table-light"><tr><th>Nama</th><th>Kendaraan</th><th>Budget</th><th>Rekomendasi</th><th>Tanggal</th><th>Aksi</th></tr></thead><tbody>@forelse($histories as $h)<tr><td class="fw-semibold">{{ $h->comparison_name }}</td><td>@foreach($h->vehicles as $v)<span class="badge text-bg-light">{{ $v->manufacturer->name }} {{ $v->model }}</span>@endforeach</td><td>{{ $h->budget?'Rp '.number_format($h->budget,0,',','.'):'-' }}</td><td>{{ $h->recommendation }}</td><td>{{ $h->created_at->format('d/m/Y') }}</td><td><div class="d-flex gap-1"><a href="{{ route('history.show',$h) }}" class="btn btn-sm btn-outline-success"><i class="bi bi-eye"></i></a><a href="{{ route('history.pdf',$h) }}" class="btn btn-sm btn-outline-secondary"><i class="bi bi-file-pdf"></i></a><form method="POST" action="{{ route('history.destroy',$h) }}" class="confirm-delete">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></div></td></tr>@empty<tr><td colspan="6" class="text-center py-5">Belum ada riwayat. <a href="{{ route('search') }}">Mulai bandingkan</a>.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $histories->links() }}</div></div>@endsection

@extends('layouts.app') @section('title','Dashboard') @section('content')<div class="container py-5"><p class="text-muted mb-1">{{ now()->translatedFormat('l, d F Y') }}</p><h1 class="fw-bold">Halo, {{ auth()->user()->name }} 👋</h1><p class="text-muted">Mari cari kendaraan yang paling masuk akal hari ini.</p><div class="row g-4 my-3"><div class="col-md-4"><div class="card p-4"><div class="icon-bubble"><i class="bi bi-bar-chart"></i></div><h2 class="mt-3">{{ $historyCount }}</h2><span class="text-muted">Riwayat perbandingan</span></div></div><div class="col-md-4"><div class="card p-4"><div class="icon-bubble"><i class="bi bi-heart"></i></div><h2 class="mt-3">{{ $favoriteCount }}</h2><span class="text-muted">Kendaraan favorit</span></div></div><div class="col-md-4"><div class="card p-4 bg-ec text-white"><div class="icon-bubble bg-white"><i class="bi bi-lightning"></i></div><h4 class="mt-3">Quick Compare</h4><a href="{{ route('search') }}" class="text-warning">Mulai sekarang <i class="bi bi-arrow-right"></i></a></div></div></div><div class="row g-4 mt-3"><div class="col-lg-8"><div class="d-flex justify-content-between"><h3>Perbandingan terbaru</h3><a href="{{ route('history') }}">Lihat semua</a></div><div class="card overflow-hidden"><table class="table mb-0"><thead><tr><th>Nama</th><th>Kendaraan</th><th>Skor</th><th></th></tr></thead><tbody>@forelse($histories as $h)<tr><td>{{ $h->comparison_name }}<br><small class="text-muted">{{ $h->created_at->diffForHumans() }}</small></td><td>{{ $h->vehicles->count() }}</td><td><span class="badge text-bg-success">{{ number_format($h->economic_score,1) }}</span></td><td><a href="{{ route('history.show',$h) }}" class="btn btn-sm btn-light">Detail</a></td></tr>@empty<tr><td colspan="4" class="text-center py-4 text-muted">Belum ada riwayat.</td></tr>@endforelse</tbody></table></div></div><div class="col-lg-4"><div class="d-flex justify-content-between"><h3>Notifikasi</h3><a href="{{ route('notifications') }}">Semua</a></div>@forelse($notifications as $n)<div class="card p-3 mb-2"><strong>{{ $n->title }}</strong><small class="text-muted">{{ Str::limit($n->content,80) }}</small></div>@empty<div class="card p-4">Tidak ada notifikasi.</div>@endforelse</div></div>@if($recommended->count())<h3 class="mt-5">Untuk Anda</h3><div class="row g-4">@foreach($recommended as $vehicle)<div class="col-md-6 col-lg-3">@include('partials.vehicle-card')</div>@endforeach</div>@endif</div>@endsection

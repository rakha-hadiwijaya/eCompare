@extends('layouts.admin')
@section('title','Notifikasi')
@section('admin-content')<div class="d-flex justify-content-between"><div><h1 class="fw-bold">Notifikasi</h1><p class="text-muted">Terbitkan informasi untuk seluruh pengguna.</p></div><a href="{{ route('admin.notifications.create') }}" class="btn btn-ec align-self-start"><i class="bi bi-plus"></i> Buat</a></div><div class="card overflow-hidden mt-3"><table class="table mb-0"><thead><tr><th>Judul</th><th>Isi</th><th>Status</th><th>Tanggal</th><th>Aksi</th></tr></thead><tbody>@foreach($notifications as $n)<tr><td class="fw-semibold">{{ $n->title }}</td><td>{{ Str::limit($n->content,100) }}</td><td><span class="badge {{ $n->status?'text-bg-success':'text-bg-secondary' }}">{{ $n->status?'Aktif':'Draft' }}</span></td><td>{{ $n->created_at->format('d/m/Y') }}</td><td><div class="d-flex gap-1"><a href="{{ route('admin.notifications.edit',$n) }}" class="btn btn-sm btn-outline-success"><i class="bi bi-pencil"></i></a><form class="confirm-delete" method="POST" action="{{ route('admin.notifications.destroy',$n) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></div></td></tr>@endforeach</tbody></table></div><div class="mt-3">{{ $notifications->links() }}</div>@endsection
